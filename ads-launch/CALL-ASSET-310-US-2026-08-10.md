# USA Call asset — 888 off, 310 on — 2026-08-10

Account `496-715-1855` only. AU untouched. Brand campaigns not re-enabled. George: take **888** off ads now; use **(310) 730-9126**.

## Numbers

| Asset ID | Phone | Class |
|----------|-------|-------|
| `404927491581` | `(310) 730-9126` | **use this** (`3107309126`) |
| `405704361176` | `888-954-8644` | 888 — pause (created earlier tonight) |
| `49435983302` | `(888) 964-8644` | 888 — already paused |
| `49435983308` | `(888) 964 8644` | 888 — already paused (museum) |

## What was paused (888)

**954 (this mutate):** the only serving 888 after tonight’s earlier swap.

| Link | Campaign | Asset | Result |
|------|----------|-------|--------|
| `24117249292~405704361176~CALL` | `VC_US_S_CORE` | `405704361176` | **PAUSED** |
| `24117249295~405704361176~CALL` | `VC_US_S_ROLES` | `405704361176` | **PAUSED** |

**964 (already paused earlier tonight — not re-mutated):**

- Campaign Call: all 16 links already **PAUSED** (CORE + ROLES + museum Search). Includes both `49435983302` and `49435983308`.
- Ad group Call: 21 VC_* AG links on `49435983302` **PAUSED** earlier tonight.
- Account Call: `49435983302` customer_asset already **PAUSED**.

No 888 Call association found on Brand / `PM_*` / museum that was still **ENABLED**. Nothing extra to pause there.

## 310 enabled where

Reused existing asset `404927491581`. Did **not** create a new 310 asset. Did **not** attach 310 to Brand.

| Link | Campaign | Result |
|------|----------|--------|
| `24117249292~404927491581~CALL` | `VC_US_S_CORE` | **ENABLED** (was paused earlier tonight) |
| `24117249295~404927491581~CALL` | `VC_US_S_ROLES` | **ENABLED** (was paused earlier tonight) |

Account-level 310 customer_asset `404927491581~CALL` left **ENABLED** (intentional).

## API ops

Toolkit: `~/Developer/shoutgeorge-ads/scripts/enable_vc_us_call_310.py` (`.venv` + gitignored `.env`). No tokens copied into this repo.

| Op | Service | Items | Result |
|----|---------|------:|--------|
| Pause 954 + enable 310 on VC_* campaign Call | `CampaignAssetService.mutate` | 4 | 2× PAUSED 954, 2× ENABLED 310 |
| Verify Search (first attempt) | `GoogleAdsService.Search` | 1 | **failed** — GAQL `(` in WHERE; no inventory change |
| Verify Search (retry, simple CALL query) | `GoogleAdsService.Search` | 1 | VC_* + all campaign Call rows |

No `RESOURCE_EXHAUSTED`. AU not queried. No campaign status changes. No Brand re-enable.

Dump (toolkit, not this repo): `shoutgeorge-ads/output/vc-us-call-asset-310-mutate.json`.

## Verify

`VC_*` campaign Call:

| Campaign | Phone | Asset | Link |
|----------|-------|-------|------|
| `VC_US_S_CORE` | `(310) 730-9126` | `404927491581` | **ENABLED** |
| `VC_US_S_ROLES` | `(310) 730-9126` | `404927491581` | **ENABLED** |
| CORE + ROLES | `888-954-8644` | `405704361176` | **PAUSED** |
| CORE + ROLES | `(888) 964-8644` | `49435983302` | **PAUSED** |

Digit check on live VC_* Call: **`3107309126`**.

## Leftover ENABLED 888

**None** at campaign Call — including Brand / `PM_*` / museum. All remaining 888 campaign links are **PAUSED** (museum + VC_* 964/954).

AG 964 still **PAUSED** from earlier tonight (not re-checked this verify; verify was campaign Call only). No AG 954 was ever created. Account 964 still **PAUSED**. Account 310 still **ENABLED** (not 888).

Ads UI library will still **list** 964 + 954 Call assets — not deleted, just not serving.

Not committed. No email.
