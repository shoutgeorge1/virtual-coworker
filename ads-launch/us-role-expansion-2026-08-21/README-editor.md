# Editor files (21 Aug 2026)

## Upload / Post
- `EDITOR-IMPORT-one-shot.csv` — new groups + TF RSA top-ups (already imported if you Posted)

## Cleanup (import next)
- `EDITOR-PAUSE-soft-generics.csv` — pauses soft/generic **positives** that leak job-seekers

## About “job” keywords
The one-shot sheet had **zero positives** containing the word `job`.
Rows like `executive assistant job` were **ad-group negatives** (`Negative = Yes`) to block applicants.
Do not pause those negatives — leave them Enabled as negatives.

Shared list `VC_US_S_🚫_JobSeekers` should already sit on `VC_US_S_ROLES`.
